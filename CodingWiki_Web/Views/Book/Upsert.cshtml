@model CodingWiki_Model.ViewModels.BookVM
@{
    var title = Model.Book.BookId == 0 ? "Create Book" : "Edit Book";
    var price = Model.Book.Price == 0 ? "" : Model.Book.Price.ToString();
}
<form method="post">
    <div class="row p-3 border">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="Book.BookId" />
        <div class="col-12 border-bottom">
            <h2 class="text-primary">@title</h2>
        </div>
        <div class="col-8 pt-4">
            <div class="form-group row py-2">
                <div class="col-4">
                    <label asp-for="Book.Title"></label>
                </div>
                <div class="col-8">
                    <input asp-for="Book.Title" class="form-control" />
                    <span asp-validation-for="Book.Title" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group row py-2">
                <div class="col-4">
                    <label asp-for="Book.ISBM"></label>
                </div>
                <div class="col-8">
                    <input asp-for="Book.ISBM" class="form-control" />
                    <span asp-validation-for="Book.ISBM" class="text-danger"></span>
                </div>
            </div>

            <div class="form-group row py-2">
                <div class="col-4">
                    <label asp-for="Book.Price"></label>
                </div>
                <div class="col-8">
                    <input asp-for="Book.Price" class="form-control" value=@price />
                    <span asp-validation-for="Book.Price" class="text-danger"></span>
                </div>
            </div>


            <div class="form-group row py-2">
                <div class="col-4">
                    <label asp-for="Book.Publisher_Id"></label>
                </div>
                <div class="col-8">
                    <select asp-for="Book.Publisher_Id" class="form-select" asp-items="@Model.PublisherList">
                        <option disabled value="0" selected>--Select Villa--</option>
                    </select>
                    <span asp-validation-for="Book.Publisher_Id" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group row py-2">
                <div class="col-4 offset-4">
                    @if (Model.Book.BookId == 0)
                    {
                        <button type="submit" class="btn btn-primary form-control">Create</button>
                    }
                    else
                    {
                        <button type="submit" class="btn btn-primary form-control">Update</button>
                    }
                </div>
                <div class="col-4">
                    <a asp-action="BookIndex" class="btn btn-success form-control">Back to List</a>
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

}